{
  "name": "tinykit-cdn-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for TinyKit CDN (R2 access)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250110.0",
    "wrangler": "^4.45.1"
  }
}
